/// Timetable Editor — Action Types (kept for future use)
///
/// Undo/redo is not implemented in this pass. These types are kept as stubs
/// so the file reference compiles. They can be removed entirely if needed.
library;

// No actions currently used — undo/redo dropped for this release.
